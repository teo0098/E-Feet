// import { useAuthSignInWithPopup } from '@react-query-firebase/auth';
// import { GoogleAuthProvider, FacebookAuthProvider, TwitterAuthProvider } from 'firebase/auth';

// import { auth } from '@utils/firebase';

// type Provider = GoogleAuthProvider | FacebookAuthProvider | TwitterAuthProvider;

// type SignInFunction = ({ provider }?: { provider?: Provider | undefined }) => Promise<void>;

const useSocials = () => {
  //   const { mutate, isError, isLoading, isSuccess, data, error } = useAuthSignInWithPopup(auth);
  //   const handleSignIn: SignInFunction = async ({ provider = new GoogleAuthProvider() } = {}) =>
  //   {
  //     mutate({ provider });
  //   };
  //   return { handleSignIn, isError, isLoading, isSuccess, data, error };
};

export default useSocials;
